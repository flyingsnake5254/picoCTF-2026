## 題目

https://learn.cylabacademy.org/library/22?page=4&category=5

f I told you a word started with 0x70 in hexadecimal, what would it start with in ASCII?


## 解題

直接輸出 `0x70` 的 ASCII 對應字元即可：

```python
print(chr(0x70))
```

輸出：`p`

得到 flag :  

```bash
picoCTF{p}
```